# steampipe-plugin-k8s

A steampipe plugin for kubernetes.   WIP....

To build, run `make`.  Copy the `config/k8s.spc` file to `~/.steampipe/config/` to create a  connection.  Currently, uses kubectl current context.